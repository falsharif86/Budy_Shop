# API base URL for the Budy backend
VITE_API_BASE_URL=https://api.budy.app

# Dev mode: fallback tenant when no subdomain detected (localhost)
DEV_TENANT_ID=8dd3f325-a5c7-d668-6cf6-3a1e13a071bb
DEV_TENANT_NAME=Weed365

# Google Sign-In client ID for web (shop)
GOOGLE_SHOP_CLIENT_ID=412447812426-r7e1kttqmgtc8iqm91ej8k2pmn77tuem.apps.googleusercontent.com

# Google Maps API key for address autocomplete (optional - falls back to manual entry)
PUBLIC_GOOGLE_MAPS_API_KEY=

# Firebase Cloud Messaging (web push notifications)
# Configure these values from Firebase Console > Project Settings > Web app
# VAPID key: Firebase Console > Cloud Messaging > Web configuration > Web Push certificates
# Note: Firebase config values are also set in src/lib/firebase.ts
